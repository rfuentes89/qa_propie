# Copiá este archivo a `.env` y completá el valor.
# `.env` está en .gitignore: nunca se versiona.
#
# Contraseña común de las 3 cuentas QA (qa.client / qa.owner / qa.agent).
# Sin esta variable, `auth.setup.ts` corta la ejecución con un mensaje claro.
PROPIE_QA_PASSWORD=
